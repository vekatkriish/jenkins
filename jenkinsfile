pipeline {
    agent { docker 'node:6.3' }
    stages {
	    properties([pipelineTriggers([pollSCM('enable')])])
        stage('build') {
            steps {
                sh 'npm --version'
		sh 'npm --version'
		sh "echo venkatesh"
            }
        }
    }
}

