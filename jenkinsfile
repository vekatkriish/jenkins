pipeline {
    agent { docker 'node:6.3' }
    stages {
        stage('build') {
	properties([pipelineTriggers([[$class: 'GitHubPushTrigger'], pollSCM('H/15 * * * *')])])
            steps {
                sh 'npm --version'
		sh 'npm --version'
            }
        }
    }
}

